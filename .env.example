# Copia este archivo a .env y completa los valores.
# No subas .env al repositorio.

PORT=3000
MONGODB_URI=mongodb://localhost:27017/rosario-central
JWT_SECRET=cambiar_por_clave_secreta_segura_en_produccion
NODE_ENV=development

# En producción, restringir CORS al dominio del frontend (ej: https://tu-app.com)
# Varios orígenes separados por coma.
# CORS_ORIGIN=https://tu-dominio.com

# Usuario administrador inicial (opcional)
# ADMIN_USER=admin
# ADMIN_PASS=admin123
